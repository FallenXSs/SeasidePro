# followerfinder
```
# KURULUM 
  ```

Pkg install git 
  ```

apt update && upgrade
   ```

pkg install openssl
   ```

pkg install python 
   ```
git clone https://github.com/FallenXSs/followerfinder
